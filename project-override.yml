services:
  dev:
    volumes:
      - ${PROJECT_DIR}:/home/dev/${PROJECT_NAME}
      - ${BASHRC_FILE}:/home/dev/.bashrc.personal:ro
      - ~/.claude:/home/dev/.claude
      - ~/.claude.json:/home/dev/.claude.json
    environment:
      - GITHUB_TOKEN
